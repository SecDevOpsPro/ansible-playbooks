---
# Docker Installation Playbook
# Description: Install Docker Engine and Docker Compose
# Tested on: Ubuntu 20.04/22.04, Debian 11, CentOS 8, RHEL 9
# Author: SecDevOpsPro

- name: Install Docker and Docker Compose
  hosts: all
  become: true
  gather_facts: true

  vars:
    docker_edition: ce
    docker_compose_version: "2.24.0"
    docker_users:
      - "{{ ansible_user }}"
    docker_daemon_options:
      storage-driver: overlay2
      log-driver: json-file
      log-opts:
        max-size: "100m"
        max-file: "3"

  pre_tasks:
    - name: Check if running on supported OS
      fail:
        msg: "This playbook only supports Ubuntu, Debian, CentOS, and RHEL"
      when: ansible_os_family not in ['Debian', 'RedHat']

  roles:
    - common
    - docker

  tasks:
    - name: Install Docker SDK for Python
      pip:
        name:
          - docker
          - docker-compose
        state: present
        executable: pip3

    - name: Add users to docker group
      user:
        name: "{{ item }}"
        groups: docker
        append: yes
      loop: "{{ docker_users }}"
      when: docker_users is defined

    - name: Create Docker daemon configuration
      copy:
        content: "{{ docker_daemon_options | to_nice_json }}"
        dest: /etc/docker/daemon.json
        owner: root
        group: root
        mode: '0644'
      notify: restart docker

    - name: Enable Docker service
      systemd:
        name: docker
        enabled: yes
        state: started

    - name: Install Docker Compose standalone
      get_url:
        url: "https://github.com/docker/compose/releases/download/v{{ docker_compose_version }}/docker-compose-linux-{{ ansible_architecture }}"
        dest: /usr/local/bin/docker-compose
        mode: '0755'
        owner: root
        group: root

    - name: Create symbolic link for docker-compose
      file:
        src: /usr/local/bin/docker-compose
        dest: /usr/bin/docker-compose
        state: link

  handlers:
    - name: restart docker
      service:
        name: docker
        state: restarted

  post_tasks:
    - name: Verify Docker installation
      command: docker --version
      register: docker_version
      changed_when: false

    - name: Verify Docker Compose installation
      command: docker-compose --version
      register: compose_version
      changed_when: false

    - name: Display versions
      debug:
        msg:
          - "{{ docker_version.stdout }}"
          - "{{ compose_version.stdout }}"

    - name: Test Docker with hello-world
      docker_container:
        name: hello-world-test
        image: hello-world
        state: started
        detach: no
        cleanup: yes
      register: hello_world
      ignore_errors: true

    - name: Show Docker test result
      debug:
        msg: "Docker is working correctly!"
      when: hello_world is succeeded
